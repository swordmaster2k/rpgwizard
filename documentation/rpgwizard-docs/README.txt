Software Requirements:
	* Node and NPM build tool: https://nodejs.org/en/

Building:
	* Run "npm install" in this directory, see "package.json" for dependencies.

Doc Generation:
	* Run "npm run docs" to generate the "docs" directory.
		* See "scripts" section of "package.json" for more details.